<!DOCTYPE html>
<html lang="es">
  <head>
    <%- include('../partials/head', {title: 'Edición de Producto'}) %>
    <link rel="stylesheet" href="/css/register.css" />
  </head>
  <body>
    <%- include('../partials/header') %>

    <main>
      <div>
        <h2 class="reg-form-main reg-span">
          Estás editando el producto: <%= product.name %>
        </h2>
      </div>
      <div class="reg-form-main reg-flex reg-flex-center">
        <form
          action="/products/<%= product.id %>?_method=put"
          method="post" enctype="multipart/form-data"
          class="reg-grow-70 reg-md-flex"
        >
          <div class="reg-mb-15 reg-md-prod">
            <label for="name">Nombre:</label>
            <input
              type="text"
              name="name"
              id="name"
              placeholder="nombre del producto"
              value="<%= product.name %>"
              class="reg-form-input"
            />
          </div>

          <div class="reg-mb-15 reg-md-prod">
            <label for="price">Precio:</label>
            <input
              type="number" step="0.01"
              name="price"
              id="price"
              placeholder="123.45"
              value="<%= product.price %>"
              class="reg-form-input"
            />
          </div>

          <div class="reg-mb-15 reg-md-prod">
            <label for="brand_id">Marca:</label>
            <select name="brand_id" id="brand_id" class="reg-form-input">
              <option value="" <% if (product.brand_id=="") { %>
                selected <% } %>>Elegí una marca
              </option>
              <option value="asus" <% if (product.brand_id=="asus") { %>
                selected <% } %>>ASUS
              </option>
              <option value="lenovo" <% if (product.brand_id =="lenovo") { %>
                selected <% } %>>Lenovo
              </option>
              <option value="apple" <% if (product.brand_id=="apple") { %>
                selected <% } %>>Apple
              </option>
              <option value="corsair" <% if (product.brand_id=="corsair") { %>
                selected <% } %>>Corsair
              </option>
              <option value="razer" <% if (product.brand_id=="razer") { %>
                selected <% } %>>Razer
              </option>
            </select>
          </div>

          <div class="reg-mb-15 reg-md-prod">
            <label for="category_id">Categoría:</label>
            <select name="category_id" id="category_id" class="reg-form-input">
              <option value="" <% if (product.category_id=="") { %>
               selected
              <% } %>>Elegí una categoría</option>
              <option value="celulares" <% if (product.category_id=="celulares") { %>
               selected  <% } %>>Celulares</option>
              <option value="pcs" <% if (product.category_id=="pcs") { %>
               selected
              <% } %>>PCs</option>
              <option value="accesorios" <% if (product.category_id=="accesorios") { %>
               selected
              <% } %>>Accesorios</option>
            </select>
          </div>

          <div class="reg-mb-15 reg-md-prod">
            <label>Descripción:</label>
            <textarea
              name="description"
              maxlength="500"
              placeholder="describi el producto"
                class="reg-form-input"
            ><%= product.description %></textarea>
          </div>

          <div class="reg-mb-15 reg-md-prod">
            <label for="image">Imagen del producto:</label>
            <input
              type="file"
              name="image"
              id="image"
              class="reg-form-input"
            />
          </div>

          <!-- <div class="reg-mb-15 reg-md-prod">
           <label for="masBuscado">Producto mas buscado?:</label>
            <select name="masBuscado" id="masBuscado" class="reg-form-input">
              <option value="" <% if (product.masBuscado=="") { %>
               selected
              <% } %>>Producto mas buscado?:</option>
              <option value="si" <% if (product.masBuscado=="si") { %>
               selected  <% } %>>Si</option>
              <option value="no" <% if (product.masBuscado=="no") { %>
               selected
              <% } %>>No</option>
            </select>
          </div> -->
          <hr class="reg-linea">
                                        
          <div class="reg-mb-15 reg flex reg-flex-center" >
            <button type="submit" class="reg-button">Guardar cambios</button>

            <button type="reset" class="reg-button-login">Cancelar</button>
          </div>
        </form>
      </div>
    </main>

    <footer class="h-foot"><%- include('../partials/footer') %></footer>
  </body>
</html>
